import * as React from 'react';
import TextInput from '../TextInput';

export default (
    <TextInput 
      uxpId="text-input"
      placeholder="placeholder"
      style={{
        height: 40,
        margin: 12,
        borderWidth: 1,
        padding: 10,
      }}
    />
)