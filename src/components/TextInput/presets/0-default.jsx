import * as React from "react";
import TextInput from "../TextInput";

export default (
  <TextInput
    uxpId="text-input"
    placeholder="Placeholder Text ..."
    style={{
      height: 40,
      borderWidth: 1,
      padding: 10,
    }}
  />
);
