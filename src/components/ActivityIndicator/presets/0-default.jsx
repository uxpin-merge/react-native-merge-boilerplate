import * as React from 'React';
import ActivityIndicator from '../ActivityIndicator';
import View from '../../View/View';

export default (
    <View uxpId="view1">
        <ActivityIndicator uxpId="spinner" />
    </View>
)